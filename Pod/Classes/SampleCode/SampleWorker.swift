import Foundation

class SampleWorker {
    func doSomeWork() -> SampleModel {
        let model = SampleModel(
            sampleTitle: "My sample is working so well!! =]"
        )
        return model
    }
}
